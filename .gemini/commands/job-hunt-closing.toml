description = "Generate a final-round closing manual (stakeholder map + business framing + closing moves)"
prompt = """
Load the router at `.claude/skills/job-hunt/SKILL.md`. Then execute the workflow in `modes/closing.md` with the user's input as the company name.
"""
