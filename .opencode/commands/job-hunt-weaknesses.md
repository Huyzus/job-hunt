---
description: Analyze patterns from interview debriefs (prescription card by default, --full for evidence appendix)
---

Read `.claude/skills/job-hunt/SKILL.md` to load the router. Then read `modes/weaknesses.md` and execute its workflow. Honor `--full` and `--evidence N` flags from the user input.
